spring.application.name=backend

# Database Configuration
spring.datasource.url=jdbc:postgresql://localhost:5432/fish_db
spring.datasource.username=postgres
spring.datasource.password=postgres
spring.datasource.driver-class-name=org.postgresql.Driver

# JPA Configuration
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.properties.hibernate.format_sql=true

# Cache (Redis)
spring.cache.type=redis
spring.cache.redis.time-to-live=600000
spring.data.redis.host=${REDIS_HOST:localhost}
spring.data.redis.port=${REDIS_PORT:6379}
spring.data.redis.password=${REDIS_PASSWORD:}
spring.data.redis.timeout=2000

server.port=8083

# JWT secret key (Base64-encoded)
app.jwt.secret=Zmlyc2gtYnJlZWRpbmctc3lzdGVtLXN1cGVyLXNlY3VyZS1qd3Qta2V5LWNoYW5nZS1pbi1wcm9k

# Dev-only: disable Redis session checks (set true in production)
app.session.store.enabled=false

# Cloudinary Configuration (read from environment variables)
my.cloudinary.cloud_name=ddxmpcyha
my.cloudinary.api_key=362525863368751
my.cloudinary.api_secret=L8decpgGELdB2KkBcqeAgzqbQJU

# Multipart upload limits
spring.servlet.multipart.max-file-size=10MB
spring.servlet.multipart.max-request-size=10MB

# VNPay configuration (Sandbox test keys - thay bang key cua ban khi len production)
app.vnpay.tmn-code=${VNPAY_TMN_CODE}
app.vnpay.hash-secret=${VNPAY_HASH_SECRET}
app.vnpay.pay-url=${VNPAY_PAY_URL}
app.vnpay.return-url=${VNPAY_RETURN_URL:http://localhost:3001/checkout/vnpay-return}
app.vnpay.frontend-return-url=${VNPAY_FRONTEND_RETURN_URL:http://localhost:3001/checkout/vnpay-return}
# URL để VNPay gọi ngầm sang Backend cập nhật trạng thái đơn hàng và trừ kho
app.vnpay.ipn-url=${VNPAY_IPN_URL:http://localhost:8083/api/customer/checkout/vnpay/vnpay-ipn}
app.vnpay.version=2.1.0
app.vnpay.command=pay
app.vnpay.curr-code=VND
app.vnpay.locale=vn
app.vnpay.order-type=other
app.vnpay.refund-url=${VNPAY_REFUND_URL:https://sandbox.vnpayment.vn/merchant_webapi/api/transaction}

ghn.token=e1d53a8b-4b9e-11f1-a973-aee5264794df
ghn.shop-id=200244
ghn.base-url=https://dev-online-gateway.ghn.vn/shiip/public-api
# Cấu hình địa chỉ shop: Thị trấn Lim, Tiên Du, Bắc Ninh
# Tiên Du (Bắc Ninh) - DistrictID: 205, Thị trấn Lim - WardCode: 20540100
ghn.from-district-id=1729
ghn.from-ward-code=190401

ai.openrouter.api-key=${OPENROUTER_API_KEY}
ai.openrouter.model=deepseek/deepseek-chat
